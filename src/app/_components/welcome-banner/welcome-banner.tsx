import "./welcome-banner.scss";
import { JSX } from "react";

export default function WelcomeBanner() : JSX.Element{


    return (
        <div className="welcome-banner">
            show this when user is no logged in
        </div>
    );

}